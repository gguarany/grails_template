= title 'Editar $resource_singular_title'

.row
  .col-12.text-right
    = link_to $resource_path(@$resource), method: :delete, class: 'btn btn-danger' do
      = icon('trash')
      Excluir $resource_singular_title
  
  .col-12
    = render 'form'
