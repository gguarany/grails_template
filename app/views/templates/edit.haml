= title 'Editar $resource_title'

.row
  .col-12
    .card.card-body.shadow
      .col-12.text-right
        = link_to $resource_path(@$resource), method: :delete, class: 'btn btn-danger' do
          = icon('trash')
          Excluir $resource_title

      = render 'form'
