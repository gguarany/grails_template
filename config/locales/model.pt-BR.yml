'pt-BR':
  boleanos:
    'true': 'Sim'
    'false': 'Não'
  activerecord:
    errors:
      models: &models
        projeto:
          attributes:
            nome: &required
              blank: não pode ficar em branco
              required: não pode ficar em branco
              invalid: inválido
        
      messages:
        record_invalid: Há campos que não podem ficar em branco!

    models:
      user:
        one: Usuário
        other: Usuários

    attributes:
